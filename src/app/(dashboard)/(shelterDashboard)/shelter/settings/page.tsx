import SettingContainer from '@/components/(shelterdashboard)/Settings/SettingContainer';
import React from 'react';

const SettingsPage = () => {
    return (
        <SettingContainer />
    );
};

export default SettingsPage;